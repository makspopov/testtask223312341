<Window x:Class="CalculationResultViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalculationResultViewer"
        xmlns:vm="clr-namespace:CalculationResultViewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="200" Width="400"
        x:Name="window">
    <Window.DataContext>
        <vm:EnterViewModel/>
    </Window.DataContext>
    <StackPanel VerticalAlignment="Center">
        <Label Content="Логин" HorizontalAlignment="Center"></Label>
        <TextBox Width="200" HorizontalAlignment="Center" Text="{Binding Path=Login}"></TextBox>
        <Label Content="Пароль" HorizontalAlignment="Center"></Label>
        <TextBox Width="200" Text="{Binding Password}" HorizontalAlignment="Center"></TextBox>
        <Button Width="100" Content="Войти" Margin="10" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=window, Mode=OneWay}"/>
    </StackPanel>
</Window>
